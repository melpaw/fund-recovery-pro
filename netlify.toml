[build]
  command = "npm run build"
  publish = "dist/client"

# TanStack Start / Nitro emits a Netlify Functions handler under
# .netlify/functions-internal at build time when NETLIFY=true is set,
# which Netlify picks up automatically.
[build.environment]
  NODE_VERSION = "20"
